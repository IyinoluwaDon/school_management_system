import { ArrowUpRight, BookOpen, CalendarCheck, CircleDollarSign, Users } from "lucide-react";
import { useAuthStore } from "../../store/auth";

export function DashboardPage() {
  const user = useAuthStore((state) => state.user);
  const cards = [{ label: "Attendance today", value: "94.8%", change: "+2.4%", icon: CalendarCheck, tone: "mint" }, { label: "Active students", value: "1,284", change: "+38 this term", icon: Users, tone: "blue" }, { label: "Pending results", value: "24", change: "Review needed", icon: BookOpen, tone: "gold" }, { label: "Fee collection", value: "₦8.4m", change: "78% collected", icon: CircleDollarSign, tone: "purple" }];
  return <section><div className="hero-card"><div><span className="eyebrow light">School overview · 2026 / 2027</span><h2>Your school, at a glance.</h2><p>Everything important, in one calm and clear workspace.</p></div><div className="hero-orbit">N</div></div><div className="stats-grid">{cards.map(({ label, value, change, icon: Icon, tone }) => <div className="stat-card" key={label}><div className={`stat-icon ${tone}`}><Icon size={19} /></div><span>{label}</span><strong>{value}</strong><small>{change}</small></div>)}</div><div className="content-grid"><div className="panel"><div className="panel-heading"><div><span className="eyebrow">Today</span><h3>Attendance pulse</h3></div><button className="text-button">View report <ArrowUpRight size={15} /></button></div><div className="attendance-chart"><div className="chart-bars">{[62, 71, 65, 82, 78, 91, 86, 94, 89, 97, 91, 95].map((height, i) => <div className="bar-wrap" key={i}><div className="bar" style={{ height: `${height}%` }} /><small>{i + 7}am</small></div>)}</div></div></div><div className="panel activity-panel"><div className="panel-heading"><div><span className="eyebrow">Live feed</span><h3>Recent activity</h3></div><span className="live-pill"><span /> Live</span></div><div className="activity"><div className="activity-avatar mint">AM</div><div><strong>Amara Mitchell checked in</strong><small>JSS 2A · 4 minutes ago</small></div><span className="activity-tag success">Present</span></div><div className="activity"><div className="activity-avatar gold">DL</div><div><strong>Daniel Lewis arrived late</strong><small>SS 1B · 12 minutes ago</small></div><span className="activity-tag warning">Late</span></div><div className="activity"><div className="activity-avatar blue">ST</div><div><strong>Science results submitted</strong><small>Mrs. Thompson · 28 minutes ago</small></div><span className="activity-tag neutral">Review</span></div></div></div><p className="role-note">Signed in as <b>{user?.role}</b>. Dashboard data will come from the Django APIs as each module is connected.</p></section>;
}
